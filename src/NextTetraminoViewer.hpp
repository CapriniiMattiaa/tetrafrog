#include <iostream>

typedef Tetramino {
    int tetramino[4][4];
}

class NextTetraminoViewer {
    protected:
    int sizeArray;

public:
    Tetramino generateRandomTetramino(Tetramino[]);
    void arrayTetramini(Tetramino[]);
};